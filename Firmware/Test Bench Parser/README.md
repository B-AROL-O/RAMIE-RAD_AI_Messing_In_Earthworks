# Test Bench Serial Communication

This is a codeblock test bench designed to test uniparser library and the callbacks for the serial commands

It's node this way to allow testing of the firmware on an platform with file logging and printf making debug a lot easier than doing it on device.